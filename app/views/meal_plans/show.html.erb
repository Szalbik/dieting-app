<!-- app/views/meal_plans/show.html.erb -->

<div class="mx-auto" id="date_picker">
    <%= render "date_picker" %>
</div>

<div id="meal_plan_details">
    <% if @meal_plan.diet.blank? %>
        <%= render 'diet_form', locals: { meal_plan: @meal_plan } %>
    <% else %>
        <%= render 'meal_plan_details', locals: { meal_plan: @meal_plan } %>
    <% end %>
</div>

<!-- Bottom Navigation -->
<div class="fixed bottom-0 left-0 right-0 flex justify-around p-4 bg-white shadow-md" >
    <%= link_to '🥗 Twoja dieta', meal_plans_path, class: 'font-semibold text-green-600' %>
    <%= link_to '🛒 Lista zakupów', shopping_cart_path, class: 'text-gray-500' %>
</div>
