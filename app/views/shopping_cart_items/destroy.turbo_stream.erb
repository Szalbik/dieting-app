<%= turbo_stream.remove("#{shopping_cart_item_id_for(@product.category, @product)}") %>
<%= turbo_stream.remove("category_#{(@product.category.name || 'Inne').parameterize}") if category_empty?(@product.category) %>
