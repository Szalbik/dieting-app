<%= render 'shopping_cart', shopping_cart: @shopping_cart %>

<!-- Bottom Navigation -->
<div data-controller="sticky-navbar" class="fixed bottom-0 left-0 right-0 z-50 flex justify-around bg-white shadow-md border-t border-gray-200" style="transform: translateZ(0); -webkit-transform: translateZ(0); backface-visibility: hidden; -webkit-backface-visibility: hidden; will-change: transform; padding: 1rem; padding-bottom: calc(1rem + env(safe-area-inset-bottom));">
  <%= link_to '🥗 Twoja dieta', diet_set_plans_path, class: 'font-semibold text-green-600' %>
  <%= link_to '🛒 Lista zakupów', shopping_cart_path, class: 'text-gray-500' %>
</div>
